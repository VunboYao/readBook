---
description: 用户说「添加疑问点/添加问题/记录问题」时，把刚解答的知识点写入 playground 疑问点文档
alwaysApply: true
---

# 添加疑问点

当用户消息包含以下任一意图时触发（同义也算）：

- 添加疑问点
- 添加问题
- 记录问题
- 记一下这个问题
- 写入疑问点

## 动作

1. 从**当前对话刚解答完的内容**提炼一条记录（不要只记用户原话，要写清结论）。
2. 写入文件：`React/playground/notes/疑问点.md`
3. **按时间倒序追加**：插在文首说明之后、第一条 `##` 记录之前（最新在上）。
4. 日期用当天（会话中的 Today's date）；文件不存在则按下方模板新建。
5. 写完后用一句话确认路径；不要提交 git，除非用户另要求。

## 条目格式

~~~~markdown
## `技术名词`：简短标题

**日期：** YYYY-MM-DD
**相关文件：** 文件名或目录（可选；仅作来源备注，无则省略本行）

**疑问：** 一句话说明当时不懂什么

**结论：**
（精炼结论；关键对比用表格）

**相关代码：**

```tsx
// 把关键示例直接写进文档；不要链到练习文件
```
~~~~

### 标题与元信息约定

- **标题只放技术点**：`` ## `技术名词`：简短标题 ``；多个技术名词用 `/` 连接（如 `` `memo` / `useMemo` ``）。
- **日期、文件名不进标题**，写在正文元信息行（`**日期：**` / `**相关文件：**`）。
- **相关文件**仅作弱化来源备注（普通文本，非链接）；练习代码可能随时删除，**不要依赖路径可读**。

### 相关代码约定（重要）

- **禁止**用相对路径 / markdown 链接指向 `src/practice`、`src/chapters`、`src/pages` 等练习代码（文件可能被删或改光）。
- **必须**把可回顾的关键代码**直接写进文档**（结论里的小示例，或独立的 `**相关代码：**` + fenced code block）。
- 代码要自洽、短小：能说明疑问点即可，不要整文件粘贴。
- 若结论里已有完整示例，可省略单独的 `**相关代码：**` 小节，避免重复。
- 文档链接（如 `docs/React/...`）一般也不作为「相关代码」；知识点应落在本条结论/代码块里。

示例：

~~~~markdown
## `useEffectEvent`：订阅要稳、读取要新

**日期：** 2026-07-27
**相关文件：** 10-sandbox.tsx

**疑问：** …

**结论：**
…

**相关代码：**

```tsx
const onTick = useEffectEvent(() => {
  setCounter(counter + increment)
})
useEffect(() => {
  const id = setInterval(() => onTick(), 1000)
  return () => clearInterval(id)
}, [])
```
~~~~

## 约束

- 一条触发 = 一条记录；用户未指定记哪条时，默认记**最近一次讲解透的疑问点**。
- 结论要可回顾：资深前端可读，少废话，保留关键类比与对比即可。
- 不要把整段聊天原文粘贴进文档；不要改 `README.md` 堆长文。
- 若用户同时指定标题/文件路径，优先用用户指定信息（仍尽量遵守上述结构）。
