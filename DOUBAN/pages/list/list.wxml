<view class="film-list" wx:if="{{films.length > 0}}">
	<film-item film="{{film}}"
			   class="film-item"
			   wx:for="{{films}}"
			   wx:for-item="film"
			   wx:for-index="idf"
			   wx:key="{{idf}}">
	</film-item>
</view>
