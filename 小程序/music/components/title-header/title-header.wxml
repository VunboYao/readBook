<view class="header">
  <text class="title">{{title}}</text>
  <view
    wx:if="{{showMore}}"
    bindtap="onMoreTap"
    class="more"
  >
    <text>更多</text>
    <van-icon name="arrow" />
  </view>
</view>