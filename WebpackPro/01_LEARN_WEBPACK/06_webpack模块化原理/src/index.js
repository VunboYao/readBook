import format from './js/format.js'

console.log(format.dateFormat());
console.log(format.priceFormat());

const math = require('./js/math')
console.log(math.add(10,20));
console.log(math.mul(10,20));
